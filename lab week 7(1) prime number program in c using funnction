#include<stdio.h>
void isprime(int);
int main()
{
    int i;
    printf("enter a number");
    scanf("%d",&i);
    isprime(i);
    return 0;
}
void isprime(int i)
{
    int x;
    int count=0;
    for(x=1;x<=i;x++)
    {
        if(i%x==0)
        count++;
    }
    if(count==2)
    printf("prime");
    else
    printf("not prime");
}
